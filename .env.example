# =============================================================================
# GREENOPS — DEMO CONFIGURATION (FAST REACTION MODE)
# DO NOT USE IN PRODUCTION
# =============================================================================

# =============================================================================
# SECURITY
# =============================================================================

# For demo only. Change in production.
JWT_SECRET_KEY=demo_secret_key_not_for_production_use

# Simple admin password for demo login
ADMIN_INITIAL_PASSWORD=admin

JWT_EXPIRATION_HOURS=24

# Make rate limiting relaxed for demo
LOGIN_RATE_LIMIT=100
LOGIN_RATE_WINDOW=60


# =============================================================================
# ENERGY CALCULATION (EXAGGERATED FOR VISIBILITY)
# =============================================================================

# Lower idle, higher active so graph difference is obvious
IDLE_POWER_WATTS=30
ACTIVE_POWER_WATTS=150

# Slightly higher electricity cost so numbers grow faster
ELECTRICITY_COST_PER_KWH=0.25


# =============================================================================
# AGENT / HEARTBEAT — FAST DEMO MODE
# =============================================================================

# Agent considered offline quickly (10 seconds)
HEARTBEAT_TIMEOUT_SECONDS=10

# Idle triggers almost immediately
IDLE_THRESHOLD_SECONDS=1

# Backend checks status frequently
OFFLINE_CHECK_INTERVAL_SECONDS=1

